import { StyleSheet } from "react-native";

export const mainFooterStyle = StyleSheet.create({
  mainFooterContainer: {
    flex: 1,
    flexDirection: "row"
  },
  mainFooterBtn: {
    flex: 1,
    flexDirection: "row",
    borderWidth: 1,
    borderColor: "red",
    justifyContent: "center",
    alignItems: "center"
  }
});