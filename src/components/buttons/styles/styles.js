import { StyleSheet } from "react-native";

export const backButton = StyleSheet.create({
  touchable: {
    flex: 1,
    justifyContent: "center",
    height: 50,
    width: 75,
    borderWidth: 2,
    borderColor: "green"
  },
  text: {
    textAlign: "center",
  }
});